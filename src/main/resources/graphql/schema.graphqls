type Query {
    getMovie(movieId: ID!): Movie
    getAllMovies : [Movie]
}

type Movie {
    movieId: ID!
    title: String!
    director: String!
    studio: String!
    movieCast: [String!]!
    releaseYear: Int!
    poster: String!
}